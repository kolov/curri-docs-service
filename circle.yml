machine:
  services:
    - docker

dependencies:
  override:
    - sbt universal:packageZipTarball
    - docker info
    - docker build --rm=false -t eu.gcr.io/iconic-setup-91510/curriculi-service-docs .
    - docker push eu.gcr.io/iconic-setup-91510/curriculi-service-docs